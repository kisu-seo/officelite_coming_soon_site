# 프로젝트 규칙 (Project Rules)

- 이후 진행되는 모든 코드 수정 및 추가 기능 요청 사항은 **1024px 이상 desktop(데스크탑) 비율**에만 반영합니다.
- mobile(모바일) 및 tablet(태블릿) 비율에는 절대 반영하면 안 됩니다.
- CSS 수정 시 `@media (min-width: 1024px)` 등의 media query(미디어 쿼리)를 사용하여 적용 대상을 제한해야 합니다.
